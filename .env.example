# DtownBeats Discord Music Bot Environment Configuration
# Copy this file to .env and fill in your actual values

# ====== REQUIRED SETTINGS ======

# Discord Bot Token (Required)
# Get this from https://discord.com/developers/applications
BOT_TOKEN=your_discord_bot_token_here

# Bot Owner Discord User ID (Required)
# Your Discord user ID for admin access
BOT_OWNER_ID=123456789012345678

# ====== MUSICBRAINZ SETTINGS ======

# MusicBrainz API Configuration (Required for metadata)
MUSICBRAINZ_USERAGENT=YourBotName/1.0
MUSICBRAINZ_VERSION=1.0
MUSICBRAINZ_CONTACT=your_email@example.com

# ====== WEB PANEL SETTINGS ======

# Web Panel Port (Optional, default: 80)
WEB_PORT=80

# Session Secret for Web Panel Security (IMPORTANT!)
# Generate a random 32+ character string for security
# You can use: python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET=your_very_secure_random_string_here_at_least_32_chars

# Discord OAuth2 Credentials (Optional, for web panel login)
# Create a Discord app and get these from the OAuth2 section
# Redirect URI should be: http://your-domain/auth
DISCORD_CLIENT_ID=your_discord_app_client_id
DISCORD_CLIENT_SECRET=your_discord_app_client_secret

# ====== PERFORMANCE SETTINGS ======

# Maximum worker threads (Optional, default: 10)
EXECUTOR_MAX_WORKERS=10

# Pagination settings (Optional)
QUEUE_PAGE_SIZE=10
HISTORY_PAGE_SIZE=10

# Voice channel timeout in seconds (Optional, default: 60)
TIMEOUT_TIME=60

# ====== INIT SCRIPT SETTINGS ======

# Disable automatic init.sh updates (Optional, default: false)
DISABLE_INIT_UPDATE=false
AUTO_UPDATE_INIT=true

# ====== DOCKER COMPOSE EXAMPLE ======
# If using Docker Compose, you can also set these in your docker-compose.yml:
#
# version: '3.8'
# services:
#   dtownbeats:
#     image: angablade/dtownbeats:latest
#     container_name: dtownbeats
#     environment:
#       - BOT_TOKEN=your_bot_token
#       - BOT_OWNER_ID=your_user_id
#       - SESSION_SECRET=your_session_secret
#       # ... add other variables as needed
#     volumes:
#       - ./music:/app/music
#       - ./config:/app/config
#       # ... other volumes
#     ports:
#       - "3333:80"
#     restart: unless-stopped

# ====== SECURITY NOTES ======
# 
# 1. SESSION_SECRET: 
#    - MUST be changed from default for security
#    - Should be at least 32 characters long
#    - Keep it secret and don't share it
#
# 2. DISCORD_CLIENT_SECRET:
#    - Keep this secret as well
#    - Only needed if you want Discord OAuth login for web panel
#
# 3. BOT_TOKEN:
#    - Never share this token publicly
#    - Regenerate it if compromised
#
# ====== WEB PANEL URLS ======
# Once running, access these endpoints:
# 
# Health Check: http://your-server:port/health
# Metrics: http://your-server:port/metrics (owner only)
# Queues: http://your-server:port/queues
# Library: http://your-server:port/library
# Login: http://your-server:port/login